DESTROY AND KILL v31

Desktop:
1. Extract the ZIP.
2. Run START_GAME.bat.
3. Choose a car. Press P for the streamed Prague 1 scene.

Mobile controls:
- Open the game on the phone. Touch steering always works.
- Tap TILT once, hold the phone naturally, and allow motion access when asked.
- Tap TILT again to recenter. FLIP reverses the tilt axis if the phone/browser reports it backwards.
- Gas, brake/reverse, handbrake, nitro, shifting, camera, reset, Prague, and menu are all on-screen buttons.

Phone + Prague on the same Wi-Fi:
- START_GAME.bat now listens on the local network.
- Open http://YOUR-PC-IP:8765/gta_vice_city_destroy_and_kill_v31.html on the phone.
- Windows Firewall may ask you to allow Node/Python on private networks.
- Some iPhone browser versions only expose motion sensors on secure origins; touch steering remains available.

Prague v31 uses ArcGIS only as the streamed visual renderer. The car, HUD, controls, camera logic, and collision response stay in the main game, avoiding the previous disappearing-car and iframe-control issues.
