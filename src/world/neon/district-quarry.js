/* NEON CITY district placeholder: quarry
   Replaced by the district author. Registering a no-op keeps the load order
   stable and the console clean until the real district lands. */
(function(){ 'use strict';
  window.NeonDistricts.push({ id:'quarry', name:'quarry', build:function(){} });
})();
